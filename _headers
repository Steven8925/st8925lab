# st8925lab — HTTP 安全標頭 / HTTP security headers
#
# 為什麼用 _headers 而不是 Worker / Why this file and not the Worker
# ------------------------------------------------------------------
# wrangler.jsonc 的 run_worker_first 只把 /api/* 送進 Worker，其餘路徑由靜態
# 資產直接回應、完全不經過 Worker 程式碼。所以 worker/index.js 裡加標頭只會
# 影響 API 回應，對真正需要 CSP 的 HTML 頁面毫無作用。
# 改用 run_worker_first: true 會讓每一個請求都經過 Worker —— 多一層延遲與成本，
# 而且 Worker 一有 bug 就是整站掛掉。為了加標頭承擔那個風險並不划算。
#
# The Worker only sees /api/*; every other path is served straight from the
# asset store. Adding headers in Worker code would therefore not protect the
# HTML pages that actually need a CSP.
#
# CSP 的誠實說明 / An honest note on the CSP
# ------------------------------------------
# 下面的 CSP 帶著 'unsafe-inline'（不含 'unsafe-eval'）。
# 'unsafe-inline' 現階段拿不掉：每一頁都有大量 inline <style>（index.html
# 就有 263 行）與 inline <script>。所以這份 CSP **擋不住 inline 注入**，
# 它擋的是「從未授權網域載入資源」與「被嵌進別人的 iframe」。
# 那仍然是實質改善，但不要誤以為 XSS 就此免疫。要真正收緊，必須先把
# inline script 抽出（DEFECTS.md 有對應項目）。
#
# 更正紀錄（2026-09-24）：本註解原先斷言「Tailwind Play CDN 在瀏覽器端
# 編譯 CSS，必須有 'unsafe-eval'」。**那是錯的，而且我沒測就寫了。**
# 實測（.wrangler/csp_eval_test.html，真實 HTTP 來源 + 真實回應標頭）：
#   對照組 eval() 與 new Function() 皆為 EvalError -> CSP 確實生效
#   Tailwind 仍完全正常：p-4 = 16px、自訂 bg-brand-500 = rgb(249,115,22)、
#   rounded-xl = 12px、自行產生 5,130 字元 CSS
# 再以 .wrangler/csp_preflight_server.py 用候選 CSP 服務三個真實頁面，
# script-src 違規 0 筆（Tailwind / Leaflet / XLSX / Phosphor / Chart 4.5.1
# 全部正常）。故移除 'unsafe-eval'。
#
# 附帶教訓：最初我用 <meta> 標籤測，對照組顯示 eval 被「允許」——
# 那代表 meta CSP 在該來源根本沒被強制執行，測試無效。是對照組抓到的。
# **沒有對照組的安全測試，「通過」與「沒生效」無法區分。**
#
# Correction: this note previously claimed the Tailwind Play CDN requires
# 'unsafe-eval'. It does not — proven with a real origin, real response
# header, and a control that confirmed the CSP was actually enforced.
#
# cdn.tailwindcss.com 已於 2026-09-24 從 script-src 移除 —— 不是因為 CSP，
# 而是因為 Travel-Assistance/index.html 不再載入它：Tailwind 已改為建置期
# 產生 Travel-Assistance/tailwind.css。Play CDN 官方明載不適用正式環境
# （每位訪客都要下載編譯器並在自己瀏覽器裡跑一次編譯）。
# 等價性以逐元素 computed style 實測：680 個非動畫元素，差異 0。
#
# cdn.tailwindcss.com was dropped because nothing loads it any more, not
# because of the CSP. Equivalence was measured element-by-element.

/*
  # 瀏覽器不得用自己的猜測覆蓋我們宣告的 Content-Type。
  # 這是成本最低、效果最確定的一條。
  X-Content-Type-Options: nosniff

  # 禁止被嵌入 iframe（點擊劫持）。frame-ancestors 是現代做法，
  # X-Frame-Options 留著給舊瀏覽器。
  X-Frame-Options: DENY

  # 跨站導覽時只送來源網域，不送完整路徑與查詢字串。
  Referrer-Policy: strict-origin-when-cross-origin

  # 明確關閉本站不使用的裝置權限。
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()

  # 一年份 HSTS，含子網域並預載。
  # 注意：這是難以回頭的設定 —— 一旦瀏覽器記住，該網域在到期前都只能走 HTTPS。
  # 本站本來就只有 HTTPS，所以風險可接受。
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

  # 跨網域政策檔一律不允許。
  X-Permitted-Cross-Domain-Policies: none

  # 以下兩個來源是**瀏覽器實測**補上的，不是推論出來的（2026-09-24）：
  #   static.cloudflareinsights.com —— Cloudflare Web Analytics 的 beacon，
  #     由 Cloudflare 自動注入到每一頁。第一版 CSP 把它擋掉了，等於關掉了
  #     本站唯一既有的分析。（順帶更正我先前「本站沒有任何分析」的說法。）
  #   cdn.jsdelivr.net 於 style-src / font-src —— HTML 引用的是
  #     unpkg.com/@phosphor-icons/web，但該套件的 CSS 會再去 jsdelivr 取
  #     regular/thin/light/bold/fill/duotone 六份子樣式表。只看 HTML 看不出來。
  # 這兩項都是靜態審查看不到、只有真的用瀏覽器開才會發現的。
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://unpkg.com https://cdn.sheetjs.com https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://unpkg.com https://fonts.googleapis.com; img-src 'self' data: blob: https:; font-src 'self' data: https://cdn.jsdelivr.net https://unpkg.com https://fonts.gstatic.com; connect-src 'self' https://st8925lab-travel-assistance.onrender.com https://st8925lab-alarm-api.onrender.com https://st8925lab-alarm-ops.onrender.com https://cloudflareinsights.com https://*.tile.openstreetmap.org; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests
